# PostgreSQL connection (defaults match docker-compose.yaml training-db)
PG_HOST=localhost
PG_PORT=5432
PG_USER=training
PG_PASSWORD=training
PG_DATABASE=training

# Optional: use a single connection URL instead
# DATABASE_URL=postgresql://training:training@localhost:5432/training
